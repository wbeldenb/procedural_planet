***
Use nmake to make the files in the command prompt, for example:

nmake aptest.exe

The program should compile with Visual C++ 6.0 (with Service Pack 5 installed)
and there can be problems with other versions.

You will also need Microsoft Macro Assembler (MASM).
***
